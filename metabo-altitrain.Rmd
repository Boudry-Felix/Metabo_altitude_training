---
title: "Metabolomics in altitude for trained and untrained subjects"
output:
  html_notebook
params:
  ignored_features:
    label: "Features to ignore"
    value: c("echantillon", "plaine_altitude", "avant_apres", "sedentaire_sportif")
    input: text
---

```{r Configuration, include=FALSE, warning=FALSE}
# Used packages
library(tidyverse)
library(data.table)
library(janitor)

# Parsing parameters
ignored_features <- get_knit_param(params$ignored_features)
```

```{r Import}
my_data <- fread(input = "Data/full.tsv") %>%
  clean_names() %>%
  select(-any_of(ignored_features)) %>%
  Filter(f = \(x) length(unique(x)) != 1)
```

```{r Pre-processing}

```

```{r Processing}

```

```{r Statistics}

```

